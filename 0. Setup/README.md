# Setup

install SQLite

install GitHub Copilot CLI

If using Windows, update powershell to version 7 or later by typing this command in a PowerShell terminal window:

```bash
winget search --id Microsoft.Powershell --exact
```


