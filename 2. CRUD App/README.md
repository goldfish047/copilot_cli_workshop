# CRUD App

Connect to the Chinook.sqlite database using the connection string "DataSource=Chinook.sqlite;Cache=Shared;". Create a blazor server-side application in this folder that works with the chinook database. I want you to build all the crud pages for the categories table.
